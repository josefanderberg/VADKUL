// This file exists to satisfy the Firebase SDK's default search for a service worker.
// The actual messaging logic is handled via custom registration of /sw.js 
// which includes the necessary environment-specific configuration.
console.log('Firebase placeholder service worker loaded to prevent 404 logs.');
